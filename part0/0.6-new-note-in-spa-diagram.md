```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Escribe una nueva nota y presiona "Save"
    Browser->>Server: Envía la nueva nota en formato JSON (con fetch)
    Server-->>Browser: Responde con 201 Created
    Browser->>User: Actualiza la lista de notas (sin recargar la página)
